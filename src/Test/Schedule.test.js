describe('Schedule Tests', () => {
  
    it('Courses sessions should not overlap', async () => { 
        expect(1).toBe(1); 
    });
  
    it('Schedule should have a not NULL list of Courses', async () => {
        expect(1).toBe(1); 
    });

    it('Schedule should have a not NULL list of Professors', async () => {
        expect(1).toBe(1); 
    });
    
    it('Schedule should have a not NULL list of Subjects', async () => {
        expect(1).toBe(1); 
    });

});
  