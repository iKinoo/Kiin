import test from "node:test";

//Falta implementar

test('Add external subject ', async () => { 
    expect(1).toBe(1);

});

test('Modify only user-added subjects', async () => {
    expect(1).toBe(1);
});