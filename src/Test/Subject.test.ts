describe('Subject Tests', () => {
  
    it('Add external subject', async () => { 
      expect(1).toBe(1); 
    });
  
    it('Modify only user-added subjects', async () => {
      expect(1).toBe(1); 
    });
  
  });
  